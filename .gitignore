__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.claude/
.claire/
.ehukaiconnect/

# Build artifacts (compiled MQL5 binaries — sources are tracked, builds aren't)
*.ex5
*.zip

# MT5 + agent compile logs (transient build output)
*.log
ehukai-compile-*.log
docs/code-reviews/compile-*.log

# Local MT5 Strategy Tester output (reports, generated tester configs, EA CSV journals)
docs/backtests/

# Audit-time screenshot bundles (chart captures, not source)
docs/code-reviews/tda-usdjpy-*/
docs/code-reviews/visual-tda-usdjpy-*/

# Video transcripts (large binary media used as research material, not source)
docs/video-transcripts/
